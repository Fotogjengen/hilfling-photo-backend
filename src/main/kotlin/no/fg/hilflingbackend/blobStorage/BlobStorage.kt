package no.fg.hilflingbackend.blobStorage

import com.azure.storage.blob.BlobServiceClient
import org.springframework.stereotype.Repository

/*
@Repository
class BlobStorage(val blobServiceClient: BlobServiceClient) {
}

 */
