version: "2"

networks:
  traefik:
    external: true

services:
  photo-api:
    networks:
      - traefik
    labels:
      - traefik.port = 8080
      - traefik.http.routers.photo-api.rule=Host("fg.nikzy.no")
      - traefik.http.routers.registry.service=photo-api

  postgres:
    labels:
      - traefik.enable=false
